{
  "name": "聆聽的河流",
  "version": "1.0.0",
  "private": true,
  "description": "Hugo site with Pagefind search",
  "scripts": {
    "build": "npm install && hugo --gc --minify && npx pagefind --site public --output-subdir ../static/pagefind",
    "search-index": "npx pagefind --site public --output-subdir ../static/pagefind"
  },
  "devDependencies": {
    "pagefind": "^1.4.0"
  },
  "author": "",
  "license": "ISC"
}